#include <bits/stdc++.h>
using namespace std;

// TODO (tokenize): implement per the lesson description.

int main() {
    string line;
    while (getline(cin, line)) {
        if (line.empty()) continue;
        cout << "TODO" << "\n";
    }
}
